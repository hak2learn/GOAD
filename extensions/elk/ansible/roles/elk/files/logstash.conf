input {
  beats {
    port => 5044
  }
}

output {
  if [@metadata][pipeline] {
    elasticsearch {
      hosts => ["https://localhost:9200"]
      manage_template => false
      index => "%{[@metadata][beat]}-%{[@metadata][version]}"
      action => "create"
      pipeline => "%{[@metadata][pipeline]}"
      user => "elastic"
      password => "changeme"
      ssl => true
      cacert => "/etc/elasticsearch/certs/instance/instance.crt"
    }
  } else {
    elasticsearch {
      hosts => ["https://localhost:9200"]
      manage_template => false
      index => "%{[@metadata][beat]}-%{[@metadata][version]}"
      action => "create"
      user => "elastic"
      password => "changeme"
      ssl => true
      cacert => "/etc/elasticsearch/certs/instance/instance.crt"
    }
  }
}
